<Window x:Class="FixIssue256.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:oxy="http://oxyplot.org/wpf"
        Title="MainWindow" Height="350" Width="525">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="26"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>
        <oxy:PlotView Grid.Row="0" Model="{Binding Path=heatMapPlotModel}"></oxy:PlotView>
        <Button x:Name="NextTestButton" Grid.Row="1" Click="NextTestButtonClick" Width="60">Next Test</Button>
    </Grid>
</Window>
