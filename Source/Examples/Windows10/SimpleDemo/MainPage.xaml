<Page
    x:Class="SimpleDemo.MainPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:SimpleDemo"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:windows="using:OxyPlot.Windows"
    mc:Ignorable="d">
    <Page.DataContext>
        <local:MainViewModel/>
    </Page.DataContext>
    <Grid Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">
        <Grid Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">
            <Grid.RowDefinitions>
                <RowDefinition Height="*" />
                <RowDefinition Height="250" />
            </Grid.RowDefinitions>
            
            <windows:PlotView Grid.Row="0" Model="{Binding Model}" 
                              ManipulationStarted="OnPlotViewManipulationStarted"
                              ManipulationCompleted="OnPlotViewManipulationCompleted" />

            <TextBox Grid.Row="1" x:Name="outputLogging" AcceptsReturn="True" IsReadOnly="True" />
        </Grid>
    </Grid>
</Page>
