<Window x:Class="LegendsDemo.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml" xmlns:oxy="clr-namespace:OxyPlot.Wpf;assembly=OxyPlot.Wpf" xmlns:pt="clr-namespace:PropertyTools.Wpf;assembly=PropertyTools.Wpf" 
        Title="Legends demo" Height="720" Width="1280">
    <DockPanel>
        <!--<Menu DockPanel.Dock="Top">
            <MenuItem Header="File">
                <MenuItem Header="Exit" Click="Exit_Click"/>
            </MenuItem>
            <pt:EnumMenuItem SelectedValue="{Binding LegendPlacement}" Header="Placement"/>
            <pt:EnumMenuItem SelectedValue="{Binding LegendPosition}" Header="Position"/>
            <pt:EnumMenuItem SelectedValue="{Binding LegendOrientation}" Header="Orientation"/>
            <pt:EnumMenuItem SelectedValue="{Binding LegendItemOrder}" Header="ItemOrder"/>
            <pt:EnumMenuItem SelectedValue="{Binding LegendItemAlignment}" Header="ItemAlignment"/>
            <pt:EnumMenuItem SelectedValue="{Binding LegendSymbolPlacement}" Header="SymbolPlacement"/>
        </Menu>-->
        <pt:PropertyEditor SelectedObject="{Binding}" DockPanel.Dock="Left" Width="300" LabelWidth="120" ShowTabs="False" Margin="8"/>
        <Grid>
            <oxy:Plot 
            x:Name="plot1" 
            Model="{Binding Model}" 
            Background="White"/>
        </Grid>
    </DockPanel>
</Window>
