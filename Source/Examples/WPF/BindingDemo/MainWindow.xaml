<Window x:Class="BindingDemo.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml" 
        xmlns:oxy="clr-namespace:OxyPlot.Wpf;assembly=OxyPlot.Wpf" 
        Title="BindingDemo" Height="480" Width="640">
    <Window.Resources>
        <XmlDataProvider x:Key="WorldPopulationData" Source="WorldPopulation.xml"  />
    </Window.Resources>
    <Grid>
        <oxy:Plot x:Name="plot1" Title="Test">
            <oxy:Plot.Series>
                <oxy:LineSeries Title="Simulated Value" DataFieldX="Time" DataFieldY="Value" StrokeThickness="2" Color="Peru" ItemsSource="{Binding Measurements}"/>
                <oxy:LineSeries Title="Maximum" DataFieldX="Time" DataFieldY="Maximum" LineStyle="Dash" StrokeThickness="2" ItemsSource="{Binding Measurements}"/>
                <oxy:LineSeries Title="Minimum" DataFieldX="Time" DataFieldY="Minimum" LineStyle="Dot" StrokeThickness="2" ItemsSource="{Binding Measurements}"/>
            </oxy:Plot.Series>
        </oxy:Plot>
    </Grid>
</Window>
