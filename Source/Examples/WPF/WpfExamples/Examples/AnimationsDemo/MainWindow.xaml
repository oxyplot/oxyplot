<Window x:Class="AnimationsDemo.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml" 
        xmlns:oxy="clr-namespace:OxyPlot.Wpf;assembly=OxyPlot.Wpf" 
        Title="AnimationsDemo" Height="480" Width="640">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="*" />
            <RowDefinition Height="auto" />
        </Grid.RowDefinitions>
        
        <oxy:PlotView Grid.Row="0" Model="{Binding PlotModel}" />

        <StackPanel Grid.Row="1" Orientation="Horizontal">
            <Label Content="Easing function" VerticalAlignment="Center" Margin="6,4,6,4" />
            <ComboBox ItemsSource="{Binding EasingFunctions}" SelectedItem="{Binding SelectedEasingFunction}" DisplayMemberPath="Name" Margin="6,4,6,4" />
            
            <Label Content="Animation duration" VerticalAlignment="Center" Margin="6,4,6,4" />
            <TextBox Text="{Binding AnimationDuration}" Margin="6,4,6,4" Height="25" />
            
            <Button Content="Animate" Click="OnAnimateClick" Width="75" Margin="6,4,6,4" />
        </StackPanel>
    </Grid>
    
</Window>