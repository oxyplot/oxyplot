<Window x:Class="OxyPlotControls.SavePlotImageDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        Title="Save Plot" SizeToContent="Height" Width="165" WindowStartupLocation="CenterOwner" WindowStyle="SingleBorderWindow" ResizeMode="NoResize" Icon="pack://application:,,,/OxyPlotControls;component/Resources/save_chart_image.ico">
    <Grid Margin="5 0">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto"/>
            <ColumnDefinition Width="Auto"/>
            <ColumnDefinition Width="Auto"/>
        </Grid.ColumnDefinitions>

        <ComboBox Name="ImageSizeComboBox" Grid.Row="0" Grid.Column="0" Grid.ColumnSpan="3" Margin="0 5" VerticalAlignment="Center" HorizontalAlignment="Stretch" SelectedIndex="0" SelectionChanged="ImageSizeComboBox_SelectionChanged">
            <ComboBoxItem Content="600 x 480 (5:4)"/>
            <ComboBoxItem Content="1000 x 800 (5:4)"/>
            <ComboBoxItem Content="600 x 450 (4:3)"/>
            <ComboBoxItem Content="1000 x 750 (4:3)"/>
            <ComboBoxItem Content="1280 x 720 (16:9)"/>
            <ComboBoxItem Content="1920 x 1080 (16:9)"/>
            <ComboBoxItem Content="800 x 800 (1:1)"/>
            <ComboBoxItem Content="1080 x 1080 (1:1)"/>
            <ComboBoxItem Content="500 x 800 (2.5:4)"/>
            <ComboBoxItem Content="Custom"/>
        </ComboBox>

        <TextBlock Text="Width: " HorizontalAlignment="Right" VerticalAlignment="Center" Grid.Column="0" Grid.Row="1"/>
        <TextBox Name="WidthTextBox" Width="80" HorizontalAlignment="Left" VerticalAlignment="Center" Margin="0 5" Grid.Column="1" Grid.Row="1" PreviewTextInput="TextBox_PreviewTextInput" PreviewKeyDown="TextBox_PreviewKeyDown" IsEnabled="False" ToolTipService.ShowOnDisabled="True"/>
        <TextBlock Text=" px" HorizontalAlignment="Left" VerticalAlignment="Center" Grid.Column="2" Grid.Row="1"/>

        <TextBlock Text="Height: " HorizontalAlignment="Right" VerticalAlignment="Center" Grid.Column="0" Grid.Row="2"/>
        <TextBox Name="HeightTextBox" Width="80" HorizontalAlignment="Left" VerticalAlignment="Center" Margin="0 5" Grid.Column="1" Grid.Row="2" PreviewTextInput="TextBox_PreviewTextInput" PreviewKeyDown="TextBox_PreviewKeyDown" IsEnabled="False" ToolTipService.ShowOnDisabled="True"/>
        <TextBlock Text=" px" HorizontalAlignment="Left" VerticalAlignment="Center" Grid.Column="2" Grid.Row="2"/>

        <!--Standard Buttons-->
        <Button Content="Save" Grid.Row="3" Grid.Column="1" Margin="0,0,0,5" Height="23" Width="80" Name="OKButton" HorizontalAlignment="Left" Click="OKButton_Click"/>
    </Grid>
</Window>
