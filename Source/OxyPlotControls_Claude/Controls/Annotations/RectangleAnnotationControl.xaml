<base:AnnotationControlBase x:Class="OxyPlotControls.Controls.Annotations.RectangleAnnotationControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:base="clr-namespace:OxyPlotControls.Controls.Base"
             xmlns:converters="clr-namespace:OxyPlotControls.Converters"
             x:Name="RectangleAnnotationControlRoot">
    <base:AnnotationControlBase.Resources>
        <converters:OxyColorConverter x:Key="OxyColorConverter"/>
    </base:AnnotationControlBase.Resources>
    <ScrollViewer VerticalScrollBarVisibility="Auto">
        <StackPanel Margin="8">
            <Expander Header="RECTANGLE ANNOTATION" IsExpanded="True">
                <Grid Margin="8">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto" MinWidth="120"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>
                    <TextBlock Grid.Row="0" Text="Text:" VerticalAlignment="Center" Margin="0,4"/>
                    <TextBox Grid.Row="0" Grid.Column="1" Text="{Binding Annotation.Text, ElementName=RectangleAnnotationControlRoot, UpdateSourceTrigger=PropertyChanged}" Margin="4"/>
                    <TextBlock Grid.Row="1" Text="Fill:" VerticalAlignment="Center" Margin="0,4"/>
                    <ComboBox Grid.Row="1" Grid.Column="1" SelectedItem="{Binding Annotation.Fill, ElementName=RectangleAnnotationControlRoot, UpdateSourceTrigger=PropertyChanged, Converter={StaticResource OxyColorConverter}}" Margin="4"/>
                    <TextBlock Grid.Row="2" Text="Stroke:" VerticalAlignment="Center" Margin="0,4"/>
                    <ComboBox Grid.Row="2" Grid.Column="1" SelectedItem="{Binding Annotation.Stroke, ElementName=RectangleAnnotationControlRoot, UpdateSourceTrigger=PropertyChanged, Converter={StaticResource OxyColorConverter}}" Margin="4"/>
                    <TextBlock Grid.Row="3" Text="Stroke Thickness:" VerticalAlignment="Center" Margin="0,4"/>
                    <TextBox Grid.Row="3" Grid.Column="1" Text="{Binding Annotation.StrokeThickness, ElementName=RectangleAnnotationControlRoot, UpdateSourceTrigger=PropertyChanged}" Margin="4"/>
                    <TextBlock Grid.Row="4" Text="Min X:" VerticalAlignment="Center" Margin="0,4"/>
                    <TextBox Grid.Row="4" Grid.Column="1" Text="{Binding Annotation.MinimumX, ElementName=RectangleAnnotationControlRoot, UpdateSourceTrigger=PropertyChanged}" Margin="4"/>
                    <TextBlock Grid.Row="5" Text="Max X:" VerticalAlignment="Center" Margin="0,4"/>
                    <TextBox Grid.Row="5" Grid.Column="1" Text="{Binding Annotation.MaximumX, ElementName=RectangleAnnotationControlRoot, UpdateSourceTrigger=PropertyChanged}" Margin="4"/>
                    <TextBlock Grid.Row="6" Text="Min Y / Max Y:" VerticalAlignment="Center" Margin="0,4"/>
                    <TextBox Grid.Row="6" Grid.Column="1" Margin="4" ToolTip="Use Annotation properties panel for Y values"/>
                </Grid>
            </Expander>
        </StackPanel>
    </ScrollViewer>
</base:AnnotationControlBase>
