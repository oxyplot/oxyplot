<base:AxisControlBase x:Class="OxyPlotControls.Controls.Axes.TimeSpanAxisControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:base="clr-namespace:OxyPlotControls.Controls.Base"
             x:Name="TimeSpanAxisControlRoot">
    <ScrollViewer VerticalScrollBarVisibility="Auto">
        <StackPanel Margin="8">
            <Expander Header="TIMESPAN AXIS" IsExpanded="True">
                <Grid Margin="8">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto" MinWidth="120"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>
                    <TextBlock Grid.Row="0" Text="Title:" VerticalAlignment="Center" Margin="0,4"/>
                    <TextBox Grid.Row="0" Grid.Column="1" Text="{Binding Axis.Title, ElementName=TimeSpanAxisControlRoot, UpdateSourceTrigger=PropertyChanged}" Margin="4"/>
                    <TextBlock Grid.Row="1" Text="Position:" VerticalAlignment="Center" Margin="0,4"/>
                    <ComboBox Grid.Row="1" Grid.Column="1" SelectedItem="{Binding Axis.Position, ElementName=TimeSpanAxisControlRoot, UpdateSourceTrigger=PropertyChanged}" Margin="4"/>
                    <TextBlock Grid.Row="2" Text="String Format:" VerticalAlignment="Center" Margin="0,4"/>
                    <TextBox Grid.Row="2" Grid.Column="1" Text="{Binding Axis.StringFormat, ElementName=TimeSpanAxisControlRoot, UpdateSourceTrigger=PropertyChanged}" Margin="4" ToolTip="e.g., 'h:mm' for hours:minutes, 'm:ss' for minutes:seconds"/>
                    <TextBlock Grid.Row="3" Text="Minimum:" VerticalAlignment="Center" Margin="0,4"/>
                    <TextBox Grid.Row="3" Grid.Column="1" Text="{Binding Axis.Minimum, ElementName=TimeSpanAxisControlRoot, UpdateSourceTrigger=PropertyChanged}" Margin="4" ToolTip="Value in seconds"/>
                </Grid>
            </Expander>
        </StackPanel>
    </ScrollViewer>
</base:AxisControlBase>
